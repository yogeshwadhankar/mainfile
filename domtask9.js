var form =document.getElementById('addforms');
var itemList =document.getElementById('items');
form.addEventListener('submit',addItems);
function addItems(e)
{
    e.preventDefault();
    var newitems =document.getElementById('item');

}
const descriptionNode =document.createTextNode(description);i
li.appendChild();